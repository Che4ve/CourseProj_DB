# JWT Secret (используется и backend, и frontend)
JWT_SECRET=your-secret-key-change-in-production

# Database (для локальной разработки)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/habit_tracker
