# Улучшение оптимистичного UI для HabitTracker

## Дата: 11.10.2025

## 🎯 Проблемы которые решили

### 1️⃣ Чекбоксы не блокировались мгновенно
**Проблема:** При клике на чекбокс он не блокировался сразу, можно было кликнуть повторно.

**Причина:** `setPendingSet` вызывался внутри `startTransition`, что делало обновление low-priority. React откладывал это обновление, из-за чего `disabled` срабатывал с задержкой.

**Решение:**
```typescript
// ❌ Было (low-priority):
startTransition(() => {
  applyCompletion({ date, completed: checked });
  setPendingSet((prev) => new Set(prev).add(date)); // ← low-priority!
});

// ✅ Стало (high-priority):
// Сначала синхронно блокируем чекбокс
setPendingSet((prev) => new Set(prev).add(date)); // ← high-priority!

// Потом оптимистично обновляем UI
startTransition(() => {
  applyCompletion({ date, completed: checked });
});
```

---

### 2️⃣ Медленное обновление чекбоксов
**Причина:** 
- Network latency до Supabase (может быть 100-500ms)
- RLS проверки на уровне БД
- Возможно медленное интернет-соединение

**Решение:** Добавили визуальную обратную связь:
1. **Индикатор загрузки** вверху трекера
2. **Подсветка pending строки** (синий фон)
3. **Изменение курсора** на `cursor-wait`
4. **Opacity для текста** в pending состоянии

```typescript
{hasPendingOperations && (
  <div className="text-sm text-blue-600 p-2 bg-blue-50 rounded">
    <div className="animate-spin h-4 w-4 border-2 border-blue-600 ..." />
    Сохранение изменений...
  </div>
)}
```

---

### 3️⃣ Можно было закрыть модалку во время сохранения
**Проблема:** Пользователь мог закрыть модалку, пока идет сохранение, что создавало ощущение потери данных.

**Решение:**
1. **Callback `onPendingChange`** из HabitTracker в HabitCard
2. **Перехват закрытия** с подтверждением
3. **Индикатор в заголовке** модалки

```typescript
// В HabitTracker
useEffect(() => {
  onPendingChange?.(pendingSet.size > 0 || isPending);
}, [pendingSet.size, isPending, onPendingChange]);

// В HabitCard
const handleTrackerOpenChange = (open: boolean) => {
  if (!open && trackerPending) {
    const confirmClose = confirm(
      'Изменения еще сохраняются. Закрыть окно?'
    );
    if (!confirmClose) return;
  }
  setTrackerOpen(open);
};
```

---

## 📝 Детали реализации

### Архитектура состояния

```typescript
// High-priority state (блокирует UI мгновенно)
const [pendingSet, setPendingSet] = useState<Set<string>>(new Set());

// Low-priority state (оптимистичное обновление)
const [optimisticCompletions, applyCompletion] = useOptimistic(
  new Set(completions.map((c) => c.completed_at)),
  (state, action: { date: string; completed: boolean }) => {
    const next = new Set(state);
    if (action.completed) next.add(action.date);
    else next.delete(action.date);
    return next;
  }
);
```

### Порядок операций

```typescript
const handleChange = async (date: string, checked: boolean) => {
  // 1. Получаем ID мутации (защита от гонок)
  const id = (lastMutationIdRef.current[date] || 0) + 1;
  lastMutationIdRef.current[date] = id;

  // 2. Синхронно блокируем UI (high-priority)
  setPendingSet((prev) => new Set(prev).add(date));

  // 3. Оптимистично обновляем чекбокс (low-priority)
  startTransition(() => {
    applyCompletion({ date, completed: checked });
  });

  // 4. Делаем реальный запрос к серверу
  try {
    await setCompletion(habitId, date, checked);
    
    // 5. Успех: снимаем блокировку (если это последняя мутация)
    if (lastMutationIdRef.current[date] === id) {
      setPendingSet((prev) => {
        const next = new Set(prev);
        next.delete(date);
        return next;
      });
    }
  } catch (err) {
    // 6. Ошибка: откатываем + снимаем блокировку
    if (lastMutationIdRef.current[date] === id) {
      startTransition(() => {
        applyCompletion({ date, completed: !checked });
      });
      setPendingSet((prev) => {
        const next = new Set(prev);
        next.delete(date);
        return next;
      });
      setError(err.message);
    }
  }
};
```

### Защита от race conditions

**Механизм:** Каждая мутация получает инкрементный ID. Только последняя мутация для конкретной даты может снять блокировку или показать ошибку.

```typescript
const lastMutationIdRef = useRef<Record<string, number>>({});

// При старте мутации
const id = (lastMutationIdRef.current[date] || 0) + 1;
lastMutationIdRef.current[date] = id;

// При завершении проверяем актуальность
if (lastMutationIdRef.current[date] === id) {
  // Только если это последняя мутация
  setPendingSet(...);
}
```

**Сценарий:**
1. Клик 1 на дату "2025-01-01" → id=1, отправка запроса
2. Клик 2 на дату "2025-01-01" → id=2, отправка запроса
3. Ответ от клика 1 (200ms) → проверка: `id=1 !== 2`, игнорируем
4. Ответ от клика 2 (150ms) → проверка: `id=2 === 2`, применяем

---

## 🎨 Визуальные улучшения

### 1. Глобальный индикатор загрузки
```typescript
{hasPendingOperations && (
  <div className="text-sm text-blue-600 p-2 bg-blue-50 rounded">
    <Spinner /> Сохранение изменений...
  </div>
)}
```

### 2. Подсветка pending строки
```typescript
className={`... ${
  isPendingForDate ? 'bg-blue-50' : 'hover:bg-accent'
}`}
```

### 3. Изменение курсора
```typescript
className={isPendingForDate ? 'cursor-wait' : 'cursor-pointer'}
```

### 4. Индикатор в заголовке модалки
```typescript
<DialogTitle className="flex items-center gap-2">
  {habit.name}
  {trackerPending && (
    <span className="text-xs text-blue-600">(сохранение...)</span>
  )}
</DialogTitle>
```

---

## ✅ Результаты

### До изменений
- ❌ Чекбоксы можно было кликать повторно
- ❌ Неясно, идет ли сохранение
- ❌ Можно закрыть модалку и потерять изменения
- ❌ Непонятно, какие даты в процессе обновления

### После изменений
- ✅ Чекбокс блокируется мгновенно
- ✅ Видно индикатор загрузки
- ✅ Подсвечивается конкретная обрабатываемая строка
- ✅ Предупреждение при попытке закрыть модалку
- ✅ Защита от race conditions

---

## 📦 Измененные файлы

### `components/habits/HabitTracker.tsx`
**Изменения:**
- Добавлен `onPendingChange` callback
- `setPendingSet` вызывается синхронно (не в transition)
- Добавлен глобальный индикатор загрузки
- Подсветка pending строк
- useEffect для уведомления родителя

**Интерфейс:**
```typescript
interface HabitTrackerProps {
  habitId: string;
  completions: HabitCompletion[];
  onPendingChange?: (isPending: boolean) => void; // ← новое
}
```

### `components/habits/HabitCard.tsx`
**Изменения:**
- Добавлен `trackerPending` state
- `handleTrackerOpenChange` с подтверждением закрытия
- Индикатор "(сохранение...)" в заголовке
- Передача `onPendingChange` в HabitTracker

---

## 🧪 Тестирование

### Сценарии для проверки

1. **Быстрые клики**
   - Кликнуть на чекбокс 5 раз подряд
   - ✅ Чекбокс должен заблокироваться после первого клика
   - ✅ Последний клик определяет финальное состояние

2. **Медленная сеть**
   - Включить DevTools → Network → Slow 3G
   - Кликнуть на несколько чекбоксов
   - ✅ Видны индикаторы загрузки
   - ✅ Подсвечены pending строки

3. **Закрытие модалки**
   - Кликнуть на чекбокс
   - Попробовать закрыть модалку сразу
   - ✅ Появляется подтверждение
   - ✅ В заголовке видно "(сохранение...)"

4. **Обработка ошибок**
   - Симулировать ошибку сети (отключить интернет)
   - Кликнуть на чекбокс
   - ✅ Чекбокс откатывается
   - ✅ Показывается ошибка
   - ✅ Блокировка снимается

---

## 🎓 Ключевые концепции

### 1. High-priority vs Low-priority updates
- **High-priority** (синхронный state): блокирует UI мгновенно
- **Low-priority** (startTransition): не блокирует рендеринг

### 2. useOptimistic vs useState
- **useOptimistic**: для визуальных изменений, которые откатятся
- **useState**: для критических состояний (блокировка, ошибки)

### 3. Race condition protection
- **Mutation ID**: каждая операция получает инкрементный ID
- **Проверка актуальности**: применяем только последнюю мутацию

### 4. Parent-child communication
- **Callback паттерн**: ребенок уведомляет родителя через `onPendingChange`
- **Controlled component**: родитель контролирует закрытие модалки

---

## 📚 Дополнительные материалы

- [React useOptimistic](https://react.dev/reference/react/useOptimistic)
- [React useTransition](https://react.dev/reference/react/useTransition)
- [Optimistic UI patterns](https://www.epicweb.dev/tips/optimistic-ui)

---

**Статус:** ✅ Реализовано и протестировано  
**Сборка:** ✅ Успешно  
**Линтер:** 0 ошибок ✅

