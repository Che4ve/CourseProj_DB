# Habit Tracker - Трекер привычек

Веб-приложение для отслеживания привычек (хороших и плохих) с ежедневным трекингом и статистикой стриков.

> ✅ **Supabase полностью настроен!** База данных готова, миграции применены, `.env.local` создан.  
> 📄 Подробности: **`SUPABASE-READY.md`** | Быстрый старт: **`QUICKSTART.md`**

## Технологический стек

- **Frontend**: Next.js 15, React 19, TypeScript
- **UI**: shadcn/ui, Tailwind CSS
- **Backend**: Next.js Server Actions, Route Handlers
- **База данных**: Supabase (PostgreSQL)
- **Авторизация**: Supabase Auth (Email + Password)
- **Тестирование**: Vitest, React Testing Library
- **CI/CD**: GitLab CI

## Функциональность

### CRUD операции для привычек
- ✅ Создание привычки (название, тип: хорошая/плохая)
- ✅ Просмотр списка всех привычек
- ✅ Редактирование привычки
- ✅ Удаление привычки

### Трекинг выполнения
- ✅ Отметка "выполнено" для каждого дня (последние 14 дней)
- ✅ Подсчет текущего стрика (streak)
- ✅ История выполнений

### Авторизация
- ✅ Регистрация через email + password
- ✅ Вход в аккаунт
- ✅ Защищенные маршруты
- ✅ Выход из аккаунта

## Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd little-web-app
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка Supabase

#### 3.1. Создайте проект в Supabase

1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Скопируйте `Project URL` и `anon public` ключ

#### 3.2. Создайте файл `.env.local`

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

#### 3.3. Настройте базу данных

Выполните следующие SQL команды в Supabase SQL Editor:

```sql
-- Создание таблицы привычек
create table habits (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) on delete cascade not null,
  name text not null,
  type text check (type in ('good', 'bad')) not null,
  created_at timestamptz default now()
);

-- Создание таблицы выполнений
create table habit_completions (
  id uuid primary key default gen_random_uuid(),
  habit_id uuid references habits(id) on delete cascade not null,
  completed_at date not null,
  unique(habit_id, completed_at)
);

-- Включение Row Level Security
alter table habits enable row level security;
alter table habit_completions enable row level security;

-- Политики для таблицы habits
create policy "Users can view their own habits"
  on habits for select
  using (auth.uid() = user_id);

create policy "Users can create their own habits"
  on habits for insert
  with check (auth.uid() = user_id);

create policy "Users can update their own habits"
  on habits for update
  using (auth.uid() = user_id);

create policy "Users can delete their own habits"
  on habits for delete
  using (auth.uid() = user_id);

-- Политики для таблицы habit_completions
create policy "Users can view completions for their habits"
  on habit_completions for select
  using (
    exists (
      select 1 from habits
      where habits.id = habit_completions.habit_id
      and habits.user_id = auth.uid()
    )
  );

create policy "Users can create completions for their habits"
  on habit_completions for insert
  with check (
    exists (
      select 1 from habits
      where habits.id = habit_completions.habit_id
      and habits.user_id = auth.uid()
    )
  );

create policy "Users can delete completions for their habits"
  on habit_completions for delete
  using (
    exists (
      select 1 from habits
      where habits.id = habit_completions.habit_id
      and habits.user_id = auth.uid()
    )
  );
```

#### 3.4. Настройка Supabase Auth

1. В Supabase Dashboard перейдите в **Authentication** → **URL Configuration**
2. Добавьте в **Redirect URLs**: `http://localhost:3000/api/auth/callback`
3. Для production добавьте: `https://your-domain.com/api/auth/callback`

### 4. Запуск приложения

#### Режим разработки

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

#### Сборка для production

```bash
npm run build
npm start
```

## Тестирование

### Запуск тестов

```bash
# Однократный запуск всех тестов
npm test

# Запуск в режиме watch (для разработки)
npm run test:watch
```

### Структура тестов

- `__tests__/components/` - юнит-тесты React компонентов
- `__tests__/actions/` - тесты Server Actions (интеграционные)

## Линтинг

```bash
npm run lint
```

## GitLab CI/CD

Проект настроен с GitLab CI пайплайном, который включает:

1. **Lint** - проверка кода с помощью ESLint
2. **Test** - запуск всех тестов
3. **Build** - проверка успешной сборки Next.js приложения

Пайплайн запускается автоматически при:
- Push в ветку `main`
- Создании Merge Request

## Структура проекта

```
/
├── app/
│   ├── (auth)/              # Страницы авторизации (login, signup)
│   ├── (dashboard)/         # Главная страница с привычками
│   ├── actions/             # Server Actions (habits, completions, auth)
│   ├── api/auth/callback/   # OAuth callback
│   ├── layout.tsx           # Root layout
│   └── globals.css          # Глобальные стили
├── components/
│   ├── auth/                # Компоненты авторизации
│   ├── habits/              # Компоненты для работы с привычками
│   └── ui/                  # shadcn/ui компоненты
├── lib/
│   ├── supabase/            # Supabase клиенты
│   ├── utils/               # Утилиты (date, cn)
│   └── types.ts             # TypeScript типы
├── __tests__/               # Тесты
│   ├── components/          # Тесты компонентов
│   └── actions/             # Тесты Server Actions
├── .gitlab-ci.yml           # GitLab CI конфигурация
├── vitest.config.ts         # Конфигурация Vitest
└── middleware.ts            # Next.js middleware (защита маршрутов)
```

## Использование

### 1. Регистрация

1. Откройте приложение
2. Перейдите на страницу регистрации
3. Введите email и пароль (минимум 6 символов)
4. После регистрации вы будете автоматически перенаправлены на главную страницу

### 2. Создание привычки

1. Нажмите кнопку "Создать привычку"
2. Введите название (например, "Пробежка утром")
3. Выберите тип: "Хорошая" или "Плохая"
4. Нажмите "Создать"

### 3. Трекинг выполнения

1. На карточке привычки нажмите "Трекинг"
2. Отметьте дни, когда привычка была выполнена
3. Текущий стрик отображается на карточке привычки (🔥 иконка)

### 4. Редактирование и удаление

- Нажмите "Изменить" для редактирования названия или типа
- Нажмите "Удалить" для удаления привычки (с подтверждением)

## Особенности реализации

### Безопасность
- Row Level Security (RLS) в Supabase обеспечивает доступ только к собственным данным
- Middleware защищает приватные маршруты
- Server Actions проверяют аутентификацию перед выполнением операций

### Производительность
- Server Components для оптимальной загрузки
- Suspense для потоковой загрузки данных
- Оптимистичные обновления UI

### UX
- Диалоговые окна для форм (не покидаем страницу)
- Валидация форм на клиенте и сервере
- Информативные сообщения об ошибках
- Индикаторы загрузки

## Авторы

Проект создан как учебный проект для демонстрации работы с современным стеком веб-разработки.

## Лицензия

MIT
